apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "collab-dashboard.name" . }}-test-connection"
  labels:
    {{- include "collab-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: registry.hrz.uni-marburg.de/dockerio-cache/busybox:latest
      command: ['wget']
      args: ['{{ include "collab-dashboard.name" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
